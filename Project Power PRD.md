# 求职找对象进度PMO管理网页 PRD需求文档

## 1\. 文档概述

### 1\.1 产品背景

为可视化跟踪、管理指定人员的求职、婚恋脱单进度，实现全流程进度管控、数据量化统计、状态自动更新，打造轻量化、精美可视化的PMO进度管理网页。支持单人详情进度编辑、全局总览、动态新增人员，无需复杂部署，可通过VS Code直接开发、本地运行使用。

### 1\.2 产品目标

- 可视化展示所有人员求职/找对象的累计时长、进度百分比、阶段状态

- 支持单人精细化进度分步管理，可自定义每阶段状态、量化数据

- 实现数据驱动状态自动变更，量化数值联动进度状态与阶段解锁

- 预留人员新增端口，支持后续无限添加求职、找对象人员

- 页面UI生动精美、交互流畅，适配电脑端使用（主打VS Code本地网页运行）

### 1\.3 适用范围

本产品为**本地轻量化网页应用**，无后端服务器、无数据库（初期本地存储），仅用于个人进度PMO管理，适配PC端浏览器打开运行，可通过VS Code直接开发、修改、预览。

## 2\. 用户与人员基础配置

### 2\.1 初始人员分类（固定初始数据，支持新增）

所有人员分为两大赛道：【求职赛道】、【找对象赛道】，系统预留新增人员空位，支持后续不限量添加。

- **求职赛道（初始2人）**：向龙一、赵陶然

- **找对象赛道（初始1人）**：黄群

### 2\.2 人员拓展规则

网页后台数据结构预留新增人员字段，支持自定义新增人员姓名、归属赛道（求职/找对象），新增后自动同步至总览面板、自动生成专属进度详情页，无需修改页面结构。

## 3\. 核心功能需求

### 3\.1 全局总览面板（首页核心页面）

网页默认首页为全员进度总览面板，以卡片形式展示所有人员的核心进度数据，清晰直观。

#### 3\.1\.1 单人员卡片展示内容

每张人员卡片统一展示以下5项核心数据：

- 人员姓名 \+ 所属赛道标签（求职/找对象）

- 累计进度天数：自动统计**从首次开启进度记录至今**的天数（支持后台手动修改起始日期）

- 整体进度百分比：根据所有阶段完成情况自动计算生成（已完成阶段/总阶段数\*100%）

- 当前核心状态：未开始/进行中/已完成（全局综合状态）

- 快捷入口：【进入详情编辑】按钮，点击跳转单人精细化进度编辑页面

#### 3\.1\.2 全局拓展功能

首页增设【新增人员】按钮，点击可填写姓名、选择赛道，一键新增进度管理卡片。

### 3\.2 单人详情进度编辑页面（核心功能页）

点击首页人员卡片即可进入对应单人详情页，支持分阶段进度管理、数据填写、状态选择，是核心操作页面。不同赛道对应**专属固定流程阶段**，阶段不可随意删减，仅可编辑状态和量化数据。

#### 3\.2\.1 求职赛道专属流程阶段（向龙一、赵陶然通用）

求职共6个固定阶段，按顺序递进，支持数据量化\+状态联动，具体如下：

1. **阶段1：撰写中英文简历**

    - 量化输入：无数字填写项（固定完成类阶段）

    - 可选状态：未开始/进行中/已完成

2. **阶段2：添加猎头联系方式**

    - 量化输入：数字输入框（填写猎头人数）

    - 状态联动规则：数值=0 → 未开始；数值≥1 → 进行中；数值≥5（默认阈值，支持后台修改）→ 已完成

3. **阶段3：与猎头/友人沟通工作机会**

    - 量化输入：数字输入框（填写沟通次数）

    - 状态联动规则：数值=0 → 未开始；数值≥1 → 进行中；数值≥10（默认阈值，支持后台修改）→ 已完成

4. **阶段4：向公司投出简历**

    - 量化输入：数字输入框（填写投递公司数量）

    - 状态联动规则：数值=0 → 未开始；数值≥1 → 进行中；数值≥20（默认阈值，支持后台修改）→ 已完成

5. **阶段5：公司一轮面试**

    - 量化输入：数字输入框（填写面试次数）

    - 状态联动规则：数值=0 → 未开始；数值≥1 → 进行中；完成对应面试即为已完成

6. **阶段6：公司二轮及终面面试**

    - 量化输入：数字输入框（填写面试次数）

    - 状态联动规则：数值=0 → 未开始；数值≥1 → 进行中；完成对应面试即为已完成

#### 3\.2\.2 找对象赛道专属流程阶段（黄群通用，支持后续新增人员复用）

结合婚恋推进通用流程，预设7个核心阶段（无特殊说明，联动规则同求职模块，阈值可自定义），阶段如下：

1. 个人形象优化（穿搭、体态、护肤等）

2. 拓展社交渠道（线上/线下社交渠道数量）

3. 新增异性联系人（量化人数）

4. 日常沟通互动（量化沟通次数）

5. 线下见面约会（量化约会次数）

6. 深度相处磨合

7. 确定恋爱关系

#### 3\.2\.3 通用阶段规则（核心AI可识别逻辑）

- **状态优先级**：数字量化值自动联动默认状态，同时支持人工手动修改状态（人工修改优先级高于自动状态），满足特殊场景需求

- **阶段解锁逻辑**：上一阶段状态为【已完成】，方可解锁下一阶段的编辑和输入权限；上一阶段未完成，下一阶段锁定不可操作

- **阈值自定义**：所有数字阶段的判定阈值（如1、5、10等）预留后台配置入口，可随时修改，无需改动前端页面

- **数据实时同步**：详情页修改数据、状态后，首页总览面板的进度百分比、全局状态实时刷新更新

### 3\.3 进度计时规则

- 首次进入单人详情页、开启任意阶段操作后，自动记录起始时间，开始计算【进度天数】

- 天数持续累计，不中断、不重置，支持手动重置起始日期

- 进度百分比 = 已完成阶段数量 / 该赛道总阶段数量 × 100%（自动精确到小数点后1位）

## 4\. 页面UI与交互需求

### 4\.1 整体风格

采用**简约高级、清新动感的现代化UI**，配色柔和治愈，卡片式布局，搭配轻微动效，整体生动精美、商务轻量化，适合长期办公使用。

### 4\.2 页面布局

- 顶部导航栏：固定顶部，展示网页名称、返回首页按钮、新增人员按钮

- 首页主体：网格卡片布局，均匀展示所有人员进度卡片，自适应页面宽度

- 详情页主体：分阶段列表布局，每个阶段独立卡片，包含阶段名称、数字输入框、状态选择器、进度标识

### 4\.3 交互动效

- 卡片悬浮动效：鼠标悬浮卡片轻微上浮、阴影加深

- 状态变色区分：未开始（灰色）、进行中（蓝色）、已完成（绿色），视觉层级清晰

- 数据修改实时刷新：输入数字、切换状态后，无刷新卡顿，实时更新进度数据

- 锁定阶段灰度显示：未解锁的下一阶段卡片灰度置灰，提示不可操作

## 5\. 数据存储与拓展需求

### 5\.1 存储方式

初期采用**浏览器本地LocalStorage存储**，无需服务器、无需数据库，关闭网页数据不丢失，适配本地轻量化使用场景。

### 5\.2 可拓展性

- 支持新增人员、新增赛道阶段

- 支持自定义各阶段量化阈值

- 预留后续对接后端、云端存储的接口位置

## 6\. 功能优先级

### P0（必须实现）

- 首页全员进度总览卡片（天数、进度、状态展示）

- 求职/找对象双赛道专属阶段流程

- 数字量化输入\+状态自动联动\+手动修改状态

- 阶段递进解锁逻辑

- 本地数据存储不丢失

### P1（优化实现）

- 精美UI动效、色彩状态区分

- 自定义进度阈值配置

- 手动修改进度起始天数

### P2（后续拓展）

- 数据导出、进度记录日志

- 云端数据同步

## 7\. 待确认细节（已默认落地，可调整）

针对未明确的细节，已预设通用合理规则，你可随时确认修改：

- 已默认各阶段量化判定阈值，支持后续自定义修改

- 已补齐找对象完整流程阶段，适配黄群及后续新增人员使用

- 默认支持人工手动覆盖自动状态，适配特殊进度场景

- 默认本地存储，满足个人轻量化使用需求

> （注：文档部分内容可能由 AI 生成）
